architecture: standalone # Single node, no replicas — lightweight for KIND

auth:
  enabled: true
  password: "redis123"

master:
  persistence:
    enabled: false # No PVC needed for local testing
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 100m
      memory: 128Mi
  service:
    type: ClusterIP
